{
  "name": "amazon-clone-mern",
  "version": "1.0.0",
  "description": "My 1st Complete Project",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}